# Examples

| Script | Shows | Needs server? |
|---|---|---|
| `publisher.php` | Wire `OutboxWebhookPublisher` with in-memory stubs and dispatch one message | No |

## Running

```bash
composer install
php examples/publisher.php
```
